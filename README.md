# FastAPI Secret Preview Project

Простий проєкт з фронтендом (HTML+CSS) та бекендом на Python (FastAPI), який демонструє роботу з секретними даними та їх маскування.

## Швидкий старт

### Встановлення залежностей

```bash
pip install -r requirements.txt
```

### Налаштування змінних середовища

1. Скопіюйте файл `.env.example` як `.env`:
```bash
copy .env.example .env
```

2. Відкрийте файл `.env` та встановіть ваш секретний ключ:
```
SECRET_KEY=your_secret_key_here
```

### Запуск бекенду

```bash
python main.py
```

Або за допомогою uvicorn:
```bash
uvicorn main:app --reload
```

Бекенд буде доступний на `http://localhost:8000`

### Запуск фронтенду

Просто відкрийте файл `index.html` у браузері або використайте локальний сервер:

```bash
# Python
python -m http.server 8080

# Або Node.js
npx http-server
```

## Структура проєкту

```
.
├── main.py              # FastAPI бекенд
├── requirements.txt     # Python залежності
├── index.html          # Фронтенд HTML
├── style.css           # Стилі для фронтенду
├── .env.example        # Приклад файлу змінних середовища
├── .gitignore          # Файли, які ігноруються Git
└── README.md           # Документація
```

## API Ендпоінти

### GET /api/info

Повертає інформацію з маскованим секретом.

**Відповідь:**
```json
{
  "message": "Інформація успішно отримана",
  "secret_preview": "Begi****End",
  "status": "success"
}
```

## Пояснення концепцій

### Що таке .env файл?

`.env` файл - це текстовий файл, який містить змінні середовища (environment variables) для вашого проєкту. Він використовується для зберігання конфігураційних даних, які можуть змінюватися залежно від середовища (розробка, тестування, продакшен).

**Приклад:**
```
SECRET_KEY=my_super_secret_key_12345
DATABASE_URL=postgresql://user:password@localhost/dbname
API_KEY=abc123xyz789
```

Переваги використання `.env` файлів:
- Централізоване зберігання конфігурації
- Легке перемикання між різними середовищами
- Відокремлення конфігурації від коду

### Чому секрети не можна комітити в репозиторій?

**Секрети НЕ повинні потрапляти в систему контролю версій (Git) з наступних причин:**

1. **Безпека**: Якщо репозиторій публічний або стане публічним, будь-хто зможе побачити ваші секрети
2. **Історія Git**: Навіть якщо ви видалите секрет пізніше, він залишиться в історії комітів
3. **Доступ команди**: Не всі члени команди повинні мати доступ до всіх секретів
4. **Різні середовища**: Секрети для розробки, тестування та продакшену повинні бути різними
5. **Відповідальність**: У разі витоку даних ви несете юридичну відповідальність

**Що робити:**
- ✅ Використовуйте `.gitignore` для виключення `.env` файлів
- ✅ Створюйте `.env.example` з прикладами (без реальних секретів)
- ✅ Використовуйте менеджери секретів (AWS Secrets Manager, HashiCorp Vault тощо)
- ✅ Ніколи не додавайте `.env` до комітів

### Що таке маскування секретів?

**Маскування секретів** - це техніка часткового приховування чутливих даних, коли потрібно показати, що секрет існує, але не розкривати його повністю.

**Приклади маскування:**
- `my_secret_key_12345` → `my_s*****2345`
- `password123` → `pass****`
- `api_key_abcdefghijk` → `api_****ijk`

**Навіщо це потрібно:**
1. **Логування**: Можна логувати запити без розкриття секретів
2. **Відлагодження**: Перевірка, що секрет завантажений, без його показу
3. **UI відображення**: Показ користувачам, що секрет налаштований
4. **Безпека**: Навіть у разі витоку логів, секрети залишаються прихованими

**У цьому проєкті:**
- Бекенд читає повний секрет з `.env`
- При відповіді клієнту показує лише масковану версію
- Формат: перші 4 символи + `****` + останні 3 символи

## 🔒 Безпека

- ✅ `.env` файл додано до `.gitignore`
- ✅ Секрети не зберігаються в коді
- ✅ Використовується маскування для відображення
- ⚠️ У продакшені обмежте CORS налаштування
- ⚠️ Використовуйте HTTPS для захисту даних

## 📝 Ліцензія

Цей проєкт створено в навчальних цілях.

